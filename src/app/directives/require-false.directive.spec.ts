import { RequireFalseDirective } from './require-false.directive';

describe('RequireFalseDirective', () => {
  it('should create an instance', () => {
    const directive = new RequireFalseDirective();
    expect(directive).toBeTruthy();
  });
});
